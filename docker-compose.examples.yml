version: '2'

services:
    zen-examples-php:
        container_name: zen-examples-php
        image: php:7.1-cli
        volumes:
            - .:/var/www
        command: /bin/bash -c "cd /var/www; /var/www/examples/run.sh"
