version: '2'

services:
    zen-php:
        container_name: zen-php
        image: php:8.0-cli-alpine3.12
        volumes:
            - .:/var/www
        working_dir: /var/www
        command: /bin/sh -c "php vendor/bin/phpunit"
